page {
  min-height: 100%;
  background: #f2f7f7;
  color: #173438;
  font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
}

button {
  font-weight: 700;
  line-height: 1.25;
}

button::after {
  border: none;
}
